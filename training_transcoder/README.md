# Transcoder Training

This folder contains the PLT training code used for the sequence transcoder model.

## Key files
- `plt_model.py` — PLT model architecture.
- `plt_module.py` — PyTorch Lightning module for PLT training.
- `run_plt.py` — Main PLT training script.

## Run
- `cd .. sh train-plt.sh`